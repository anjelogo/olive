import { JSONMember, Member } from "oceanic.js";
import ExtendedClient from "../../../Base/Client";

export const run = async (bot: ExtendedClient, member: Member, _oldMember: null | JSONMember): Promise<void> => {

  //TODO: check if user timeout state changed and log it

};